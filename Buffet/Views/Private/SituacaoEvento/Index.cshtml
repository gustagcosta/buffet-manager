@model Buffet.ViewModels.Private.SituacaoEvento.IndexViewModel;

@{
    ViewData["Title"] = "Situação de Evento";
}

<div class="card mt-2">
    <div class="card-body">
        <h3>Situação de Evento</h3>
        <form class="mt-3" method="post" asp-controller="SituacaoEvento" asp-action="Store">
            <input type="hidden" name="id" value="@Model.id">
            <div class="form-group">
                <label for="descricao">Descrição</label>
                <input required type="text" value="@Model.descricao" name="descricao" class="form-control" id="descricao">
            </div>
            <button type="submit" class="btn btn-primary">Enviar</button>
        </form>
        <div class="mt-3">
            <ul class="list-group">
                @foreach (var s in @Model.Situacoes)
                {
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        @s.Descricao
                        <span>
                            <a asp-controller="SituacaoEvento" asp-action="Edit" asp-route-id="@s.Id" class="btn btn-primary">Editar</a>
                            <a asp-controller="SituacaoEvento" asp-action="Destroy" asp-route-id="@s.Id" class="btn btn-danger">Deletar</a>
                        </span>
                    </li>
                }
            </ul>
        </div>
    </div>
</div>